# Model configuration
LLM_MODEL=llama3.2:3b  # Default recommended model

# === LLM Provider Configuration (choose one) ===

# OPTION 1: LiteLLM Configuration (takes precedence if set)
# Set these values to use your own LiteLLM deployment
LITELLM_URL=  # Example: http://your-litellm-url/v1
LITELLM_API_KEY=  # Set if your LiteLLM requires authentication

# OPTION 2: Ollama Configuration 
# Used only if LITELLM_URL is not set
# When using Docker Compose, leave this as is
OLLAMA_URL=http://ollama:11434/v1
# OLLAMA_API_KEY=  # Uncomment and set if Ollama requires authentication